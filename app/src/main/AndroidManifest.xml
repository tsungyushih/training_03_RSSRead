<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frandog.training_03_rssread">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--以下兩行重要!!!在INTENT的對象為Class時，要在<application的節點中寫註冊代碼-->
        <activity android:name =".UseDomParse"/>
        <activity android:name =".UseSaxParse"/>

        <activity android:name="com.frandog.training_03_rssread.UseXML">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>